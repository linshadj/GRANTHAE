<div
    class="glass-card group relative p-3 flex flex-col h-full overflow-hidden hover:scale-[1.02] transition-all duration-500">
    <div class="relative aspect-[2/3] w-full overflow-hidden rounded-xl bg-gray-100">
        <% let displayImg = book.coverImage || book.image || (book.images && book.images.length > 0 ? book.images[0] : '/images/default-book.jpg'); %>
        <% let displayTitle = book.name || book.title || 'Untitled'; %>
        <img src="<%= displayImg %>" alt="<%= displayTitle %>"
            class="h-full w-full object-cover object-center transition-transform duration-700 group-hover:scale-110">

        <!-- Hover Overlay -->
        <div
            class="absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center gap-2">
            <% let wishlisted = typeof wishlistProductIds !== 'undefined' && wishlistProductIds.includes(book._id.toString()); %>
            <button onclick="event.preventDefault(); event.stopPropagation(); addToWishlistGlobal('<%= book._id %>', '', this)"
                class="bg-white/90 p-2 rounded-full text-apple-dark hover:bg-white <%= wishlisted ? 'text-red-500' : 'hover:text-red-500' %> transition-colors flex items-center justify-center relative z-10"
                title="<%= wishlisted ? 'Remove from Wishlist' : 'Add to Wishlist' %>">
                <i data-lucide="heart" class="w-5 h-5 <%= wishlisted ? 'fill-current' : '' %>"></i>
            </button>
        </div>

        <div
            class="absolute top-2 right-2 bg-green-500 px-3 py-1 rounded-lg text-sm font-black text-white shadow-xl border border-white/20">
            ₹<%= book.price %>
        </div>
    </div>

    <div class="mt-4 flex flex-col flex-grow">
        <div class="flex justify-between items-start">
            <div>
                <span
                    class="inline-flex items-center rounded-md bg-gray-50 px-2 py-1 text-xs font-medium text-apple-text-gray ring-1 ring-inset ring-gray-500/10 mb-2">
                    <%= book.genre %>
                </span>
                <h3
                    class="text-base font-semibold text-apple-dark group-hover:text-apple-blue transition-colors clamp-1">
                    <a href="/product/<%= book._id %>">
                        <span class="absolute inset-0"></span>
                        <%= displayTitle %>
                    </a>
                </h3>
            </div>
        </div>
        <p class="mt-1 text-sm text-apple-text-gray">
            <%= book.author %>
        </p>

        <div class="mt-auto pt-4 flex items-center justify-between border-t border-gray-100/50">
            <div class="flex items-center">
                <i data-lucide="star" class="h-4 w-4 text-yellow-400 fill-current"></i>
                <span class="ml-1 text-sm text-apple-text-gray">
                    <%= book.rating %>
                </span>
            </div>
            <% if (book.totalStock > 0) { %>
                <div class="flex items-center text-green-600 text-xs font-medium">
                    <span class="w-2 h-2 bg-green-500 rounded-full mr-1 animate-pulse"></span>
                    Available
                </div>
            <% } else { %>
                <div class="flex items-center text-red-500 text-xs font-medium">
                    <span class="w-2 h-2 bg-red-500 rounded-full mr-1"></span>
                    Out of Stock
                </div>
            <% } %>
        </div>
    </div>
</div>